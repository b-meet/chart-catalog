const WorkInProgress = ({ section }) => {
	return (
		<section className='h-[400px] w-full p-12 flex items-center justify-center'>
			{section} work is in progress : )
		</section>
	);
};

export default WorkInProgress;
